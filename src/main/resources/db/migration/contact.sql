CREATE TABLE IF NOT EXISTS CONTACT (
    USER_ID INT REFERENCES USER1,
    NAME VARCHAR(100),
    EMAIL VARCHAR(100),
    PHONE_NO INT,
    MESSAGE VARCHAR(100),
    CONTACT_ID INT NOT NULL AUTO_INCREMENT,
    PRIMARY KEY (CONTACT_ID)
);